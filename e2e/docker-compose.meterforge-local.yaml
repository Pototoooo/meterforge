# Note: this can only be started using
#  docker compose -f docker-compose.infra.yaml -f docker-compose.meterforge.yaml up
#
# Given this is governed by the Makefile, it's fine to not to add all the extends here.
services:
  meterforge:
    build: ..

  sink-worker:
    build: ..

  billing-worker:
    build: ..
