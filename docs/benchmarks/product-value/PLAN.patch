--- PLAN.md
+++ PLAN.md
@@ -144,3 +144,11 @@
 已有：共享幂等写入技术对照、LoreLattice RAG/Agent 质量评测、用户研究中的额度与成本痛点记录。
 
 缺口：接入任务原始操作日志、异常事件回放结果、运营排障任务结果、真实或明确标注的支付漏斗数据。没有这些原始数据时，以上内容只能作为实验方案，不能写成已取得的产品效果。
+
+## 2026-09-11 本轮执行补充（以本节为准）
+
+只执行自动化产品验收：五类模型各 1,000 条唯一事件、250 条重试，三轮独立客户/事件集；两客户归属、计费模式和价格版本分组、供应端成本字段聚合；500 条消费暂停积压恢复；7 天晚到事件及业务无效输入。独立无客户绑定试测保留但不计入正式分母。
+
+主指标为分组计量与独立 oracle 一致性、重试后的数量不增、恢复后积压完整性；错误单位/负数/缺失字段为业务语义边界探针，不预先假定 CloudEvents 应禁止所有负数。主体过滤正确不等于租户鉴权安全。
+
+采用本机已存在镜像的固定哈希，未证明对应当前源码。非已部署 LoreLattice 五类模型真实调用；不调用收费模型，不发生支付，不修改两个项目的生产实现。只有三个重复运行，不估计总体 P95/99.99% 可靠性。接入耗时、运营定位效率、理解率保持待测。
