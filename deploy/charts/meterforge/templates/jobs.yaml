---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "meterforge.componentName" (list . "subscription-sync") }}
  labels:
    {{- include "meterforge.componentLabels" (list . "subscription-sync") | nindent 4 }}
spec:
  schedule: "{{ .Values.jobs.subscriptionSync.schedule }}"
  concurrencyPolicy: Forbid
  jobTemplate:
    metadata:
      annotations:
        {{- with .Values.podAnnotations }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      labels:
        {{- include "meterforge.componentLabels" (list . "subscription-sync") | nindent 8 }}
        {{- with .Values.podLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      template:
        metadata:
          annotations:
            {{- with .Values.podAnnotations }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
            checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
          labels:
            {{- include "meterforge.componentLabels" (list . "subscription-sync") | nindent 12 }}
            {{- with .Values.podLabels }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
        spec:
          restartPolicy: Never # next run is fine
          {{- with .Values.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 10 }}
          {{- end }}
          serviceAccountName: {{ include "meterforge.serviceAccountName" . }}
          securityContext:
            {{- toYaml .Values.podSecurityContext | nindent 12 }}
          {{ if or .Values.postgresql.enabled .Values.clickhouse.enabled -}}
          initContainers:
            {{- if .Values.postgresql.enabled -}}
            {{- include "meterforge.init.postgres" (list .) | nindent 12 }}
            {{- end }}
            {{- if .Values.clickhouse.enabled -}}
            {{- include "meterforge.init.clickhouse" (list .) | nindent 12 }}
            {{- end }}
          {{- end }}
          containers:
            - name: subscription-sync
              securityContext:
                {{- toYaml .Values.securityContext | nindent 16 }}
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              command: ["/entrypoint.sh", "meterforge-jobs"]
              args: [ "--config", "/etc/meterforge/config.yaml", "billing", "subscriptionsync", "all"]
              ports:
                - name: telemetry-http
                  containerPort: 10000
                  protocol: TCP
              resources:
                {{- toYaml .Values.resources | nindent 16 }}
              volumeMounts:
                - name: config
                  mountPath: /etc/meterforge
                {{- if ne (len .Values.caRootCertificates) 0 }}
                - name: ca-certificates
                  mountPath: /usr/local/share/ca-certificates
                  readOnly: true
                {{- end }}
          volumes:
            - name: config
              configMap:
                name: {{ include "meterforge.fullname" . }}
            {{- if ne (len .Values.caRootCertificates) 0 }}
            - name: ca-certificates
              configMap:
                name: ca-certificates
            {{- end }}
          {{- with .Values.nodeSelector }}
          nodeSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.affinity }}
          affinity:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.tolerations }}
          tolerations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "meterforge.componentName" (list . "billing-collect-invoices") }}
  labels:
    {{- include "meterforge.componentLabels" (list . "billing-collect-invoices") | nindent 4 }}
spec:
  schedule: "{{ .Values.jobs.billingCollectInvoices.schedule }}"
  concurrencyPolicy: Forbid
  jobTemplate:
    metadata:
      annotations:
        {{- with .Values.podAnnotations }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      labels:
        {{- include "meterforge.componentLabels" (list . "billing-collect-invoices") | nindent 8 }}
        {{- with .Values.podLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      template:
        metadata:
          annotations:
            {{- with .Values.podAnnotations }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
            checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
          labels:
            {{- include "meterforge.componentLabels" (list . "billing-collect-invoices") | nindent 12 }}
            {{- with .Values.podLabels }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
        spec:
          restartPolicy: Never # next run is fine
          {{- with .Values.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 10 }}
          {{- end }}
          serviceAccountName: {{ include "meterforge.serviceAccountName" . }}
          securityContext:
            {{- toYaml .Values.podSecurityContext | nindent 12 }}
          {{ if or .Values.postgresql.enabled .Values.clickhouse.enabled -}}
          initContainers:
            {{- if .Values.postgresql.enabled -}}
            {{- include "meterforge.init.postgres" (list .) | nindent 12 }}
            {{- end }}
            {{- if .Values.clickhouse.enabled -}}
            {{- include "meterforge.init.clickhouse" (list .) | nindent 12 }}
            {{- end }}
          {{- end }}
          containers:
            - name: billing-collect-invoices
              securityContext:
                {{- toYaml .Values.securityContext | nindent 16 }}
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              command: ["/entrypoint.sh", "meterforge-jobs"]
              args: [ "--config", "/etc/meterforge/config.yaml", "billing", "collect", "all"]
              ports:
                - name: telemetry-http
                  containerPort: 10000
                  protocol: TCP
              resources:
                {{- toYaml .Values.resources | nindent 16 }}
              volumeMounts:
                - name: config
                  mountPath: /etc/meterforge
                {{- if ne (len .Values.caRootCertificates) 0 }}
                - name: ca-certificates
                  mountPath: /usr/local/share/ca-certificates
                  readOnly: true
                {{- end }}
          volumes:
            - name: config
              configMap:
                name: {{ include "meterforge.fullname" . }}
            {{- if ne (len .Values.caRootCertificates) 0 }}
            - name: ca-certificates
              configMap:
                name: ca-certificates
            {{- end }}
          {{- with .Values.nodeSelector }}
          nodeSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.affinity }}
          affinity:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.tolerations }}
          tolerations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "meterforge.componentName" (list . "billing-advance-invoices") }}
  labels:
    {{- include "meterforge.componentLabels" (list . "billing-advance-invoices") | nindent 4 }}
spec:
  schedule: "{{ .Values.jobs.billingAdvanceInvoices.schedule }}"
  concurrencyPolicy: Forbid
  jobTemplate:
    metadata:
      annotations:
        {{- with .Values.podAnnotations }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      labels:
        {{- include "meterforge.componentLabels" (list . "billing-advance-invoices") | nindent 8 }}
        {{- with .Values.podLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      template:
        metadata:
          annotations:
            {{- with .Values.podAnnotations }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
            checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
          labels:
            {{- include "meterforge.componentLabels" (list . "billing-advance-invoices") | nindent 12 }}
            {{- with .Values.podLabels }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
        spec:
          restartPolicy: Never # next run is fine
          {{- with .Values.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 10 }}
          {{- end }}
          serviceAccountName: {{ include "meterforge.serviceAccountName" . }}
          securityContext:
            {{- toYaml .Values.podSecurityContext | nindent 12 }}
          {{ if or .Values.postgresql.enabled .Values.clickhouse.enabled -}}
          initContainers:
            {{- if .Values.postgresql.enabled -}}
            {{- include "meterforge.init.postgres" (list .) | nindent 12 }}
            {{- end }}
            {{- if .Values.clickhouse.enabled -}}
            {{- include "meterforge.init.clickhouse" (list .) | nindent 12 }}
            {{- end }}
          {{- end }}
          containers:
            - name: billing-advance-invoices
              securityContext:
                {{- toYaml .Values.securityContext | nindent 16 }}
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              command: ["/entrypoint.sh", "meterforge-jobs"]
              args: [ "--config", "/etc/meterforge/config.yaml", "billing", "advance", "all"]
              ports:
                - name: telemetry-http
                  containerPort: 10000
                  protocol: TCP
              resources:
                {{- toYaml .Values.resources | nindent 16 }}
              volumeMounts:
                - name: config
                  mountPath: /etc/meterforge
                {{- if ne (len .Values.caRootCertificates) 0 }}
                - name: ca-certificates
                  mountPath: /usr/local/share/ca-certificates
                  readOnly: true
                {{- end }}
          volumes:
            - name: config
              configMap:
                name: {{ include "meterforge.fullname" . }}
            {{- if ne (len .Values.caRootCertificates) 0 }}
            - name: ca-certificates
              configMap:
                name: ca-certificates
            {{- end }}
          {{- with .Values.nodeSelector }}
          nodeSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.affinity }}
          affinity:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.tolerations }}
          tolerations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
